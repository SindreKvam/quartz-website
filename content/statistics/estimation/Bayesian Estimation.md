Bayesian estimation is based on Bayes Theorem.
The basic idea is that we exploit prior knowledge about $\theta$.

$$
p(\boldsymbol{x},\theta)=p(\theta)p(\boldsymbol{x}|{\theta}) = p(\boldsymbol{x})p(\theta|\boldsymbol{x})
$$