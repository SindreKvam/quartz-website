
The ROC is given by plotting the probability of detection $P_D$ with the probability of false alarms $P_{FA}$. 

In the case of white gaussian noise, the ROC can be written on the following form:
$$
P_D(P_{FA})=Q(Q^{-1}(P_{FA})-d)
$$
The performance of the detector increases with $d$.
